import React, { ReactElement } from "react";
import styled from "styled-components";

const ChatHistory: React.FC = (): ReactElement => {
  return <div>Here is some histroy</div>;
};

export default ChatHistory;
